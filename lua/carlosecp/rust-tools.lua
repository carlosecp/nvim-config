-- This config file is called when the server is loaded.
-- Refer to the lsp_installer config.
return {
	hover_with_actions = false,
	inlay_hints = {
		show_parameter_hints = false,
		show_variable_name = false,
		parameter_hints_prefix = "<- ",
		other_hints_prefix = " >> "
	}
}
